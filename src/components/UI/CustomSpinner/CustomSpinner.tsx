import { CircularProgress } from "@mui/material";
import React from "react";

const CustomSpinner = () => {
  return (
    <>
      <CircularProgress />
    </>
  );
};

export default CustomSpinner;
